import { describe, it, expect } from 'vitest';

describe('Basic test', () => {
  it('true should be true', () => {
    expect(true).toBe(true);
  });
  
  it('1 + 1 should be 2', () => {
    expect(1 + 1).toBe(2);
  });
});
